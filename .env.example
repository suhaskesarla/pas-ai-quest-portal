# Copy this file to .env before running Docker Compose.
# Use a local-only password; never commit the populated .env file.
SQL_SERVER_PASSWORD=replace-with-a-strong-local-password
STUB_AUTH_SUBJECT=local-developer
STUB_AUTH_NAME=Local Developer
