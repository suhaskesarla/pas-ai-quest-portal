# Teams app package template

Replace every `{{...}}` token during tenant activation. Add the required 192×192 `color.png` and 32×32 transparent `outline.png` before packaging. `webApplicationInfo` is parameterized for future Entra SSO; this repository does not claim that SSO is implemented.
